(comment fastlisp sample code)

(comment names)
(lambda ibis
(lambda kestrel
(lambda kite

(main program)
(lambda x (lambda y (lambda z (z x y))))

(comment definitions)

(comment kite)
) (kestrel ibis)

(comment kestrel)
) (lambda x
          (lambda y
                  x))

(comment ibis)
) (lambda x
          x)

